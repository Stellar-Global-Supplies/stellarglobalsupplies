{
  "name": "api-metrics-processor",
  "version": "1.0.0",
  "description": "Process and cache API + Lambda metrics to S3 (scheduled)",
  "main": "src/handler.js",
  "scripts": {
    "build": "tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@aws-sdk/client-cloudwatch": "^3.450.0",
    "@aws-sdk/client-s3": "^3.450.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^3.0.0",
    "typescript": "^5.0.0"
  }
}