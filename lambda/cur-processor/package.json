{
  "name": "cur-processor",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npx esbuild src/handler.ts --bundle --platform=node --target=node22 --outfile=dist/handler.js --external:@aws-sdk/* --external:zlib",
    "prebuild": "rm -rf dist"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.600.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "esbuild": "^0.23.0",
    "typescript": "^5.5.0"
  }
}